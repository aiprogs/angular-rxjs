// TODO: MB we can use it for classes in elements
export enum ItemType {
  divider = 'divider',
  title = 'title',
  dropdown = 'dropdown',
  label = 'label',
  empty = 'empty',
  link = 'link',
  external_link = 'external_link'
}
